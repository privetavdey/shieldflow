# Shield Flow

Run locally over HTTP so Rive (`bg.riv`) and other assets load correctly (CORS does not allow loading from `file://`).

```bash
npm start
```

Then open **http://localhost:3000** in your browser.
